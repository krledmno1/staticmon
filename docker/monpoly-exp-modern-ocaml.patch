diff --git a/dune b/dune
index 9420d94..b94bb5c 100644
--- a/dune
+++ b/dune
@@ -4,9 +4,9 @@
 
 (env
   (dev
-    (flags :standard -warn-error -A -w -27-32..39)
+    (flags :standard -w -27-32..39)
     (js_of_ocaml (flags --pretty --no-inline --debug-info --source-map)))
   (release
-    (flags :standard -warn-error -A -w -27-32..39)
+    (flags :standard -w -27-32..39)
     (ocamlopt_flags (:standard -O3))
     (js_of_ocaml (flags --opt 3))))
diff --git a/src/cppmon.ml b/src/cppmon.ml
index 99385b8..838005d 100644
--- a/src/cppmon.ml
+++ b/src/cppmon.ml
@@ -1,4 +1,5 @@
 open MFOTL
+open Ppx_yojson_conv_lib.Yojson_conv.Primitives
 open Predicate
 open Relation
 open Helper
@@ -6,8 +7,15 @@ open Helper
 module MyZ = struct
   include Z
 
-  let yojson_of_t arg = yojson_of_int (Z.to_int arg)
-  let t_of_yojson arg = Z.of_int (int_of_yojson arg)
+  let yojson_of_t arg = `Int (Z.to_int arg)
+
+  let t_of_yojson = function
+    | `Int value -> Z.of_int value
+    | `Intlit value -> Z.of_string value
+    | json ->
+        failwith
+          (Printf.sprintf "MyZ.t_of_yojson: expected integer, got %s"
+             (Yojson.Safe.to_string json))
 end
 
 exception UnsupportedFragment of string
diff --git a/src/verified.ml b/src/verified.ml
index d355281..07adcd8 100644
--- a/src/verified.ml
+++ b/src/verified.ml
@@ -1,3 +1,5 @@
+[@@@warning "-8-11-20-26"]
+
 module Uint32 : sig
   val less : int32 -> int32 -> bool
   val less_eq : int32 -> int32 -> bool
